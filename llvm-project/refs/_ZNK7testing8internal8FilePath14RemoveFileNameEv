<dec f='llvm/llvm/utils/unittest/googletest/include/gtest/internal/gtest-filepath.h' l='132' type='testing::internal::FilePath testing::internal::FilePath::RemoveFileName() const'/>
<doc f='llvm/llvm/utils/unittest/googletest/include/gtest/internal/gtest-filepath.h' l='126'>// RemoveFileName returns the directory path with the filename removed.
  // Example: FilePath(&quot;path/to/file&quot;).RemoveFileName() returns &quot;path/to/&quot;.
  // If the FilePath is &quot;a_file&quot; or &quot;/a_file&quot;, RemoveFileName returns
  // FilePath(&quot;./&quot;) or, on Windows, FilePath(&quot;.\\&quot;). If the filepath does
  // not have a file, like &quot;just/a/dir/&quot;, it returns the FilePath unmodified.
  // On Windows platform, &apos;\&apos; is the path separator, otherwise it is &apos;/&apos;.</doc>
<use f='llvm/llvm/utils/unittest/googletest/src/gtest.cc' l='3436' u='c' c='_ZN7testing8internal24XmlUnitTestResultPrinter18OnTestIterationEndERKNS_8UnitTestEi'/>
<def f='llvm/llvm/utils/unittest/googletest/src/gtest-filepath.cc' l='166' ll='175' type='testing::internal::FilePath testing::internal::FilePath::RemoveFileName() const'/>
<use f='llvm/llvm/utils/unittest/googletest/src/gtest-filepath.cc' l='317' u='c' c='_ZNK7testing8internal8FilePath28CreateDirectoriesRecursivelyEv'/>
<doc f='llvm/llvm/utils/unittest/googletest/src/gtest-filepath.cc' l='160'>// RemoveFileName returns the directory path with the filename removed.
// Example: FilePath(&quot;path/to/file&quot;).RemoveFileName() returns &quot;path/to/&quot;.
// If the FilePath is &quot;a_file&quot; or &quot;/a_file&quot;, RemoveFileName returns
// FilePath(&quot;./&quot;) or, on Windows, FilePath(&quot;.\\&quot;). If the filepath does
// not have a file, like &quot;just/a/dir/&quot;, it returns the FilePath unmodified.
// On Windows platform, &apos;\&apos; is the path separator, otherwise it is &apos;/&apos;.</doc>
