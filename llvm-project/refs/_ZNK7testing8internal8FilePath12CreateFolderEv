<dec f='llvm/llvm/utils/unittest/googletest/include/gtest/internal/gtest-filepath.h' l='150' type='bool testing::internal::FilePath::CreateFolder() const'/>
<doc f='llvm/llvm/utils/unittest/googletest/include/gtest/internal/gtest-filepath.h' l='146'>// Create the directory so that path exists. Returns true if successful or
  // if the directory already exists; returns false if unable to create the
  // directory for any reason, including if the parent directory does not
  // exist. Not named &quot;CreateDirectory&quot; because that&apos;s a macro on Windows.</doc>
<use f='llvm/llvm/utils/unittest/googletest/src/gtest-filepath.cc' l='318' u='c' c='_ZNK7testing8internal8FilePath28CreateDirectoriesRecursivelyEv'/>
<def f='llvm/llvm/utils/unittest/googletest/src/gtest-filepath.cc' l='325' ll='341' type='bool testing::internal::FilePath::CreateFolder() const'/>
<doc f='llvm/llvm/utils/unittest/googletest/src/gtest-filepath.cc' l='321'>// Create the directory so that path exists. Returns true if successful or
// if the directory already exists; returns false if unable to create the
// directory for any reason, including if the parent directory does not
// exist. Not named &quot;CreateDirectory&quot; because that&apos;s a macro on Windows.</doc>
