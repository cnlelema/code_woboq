<dec f='llvm/llvm/lib/Target/ARM/ARMBaseInstrInfo.h' l='58' type='bool llvm::ARMBaseInstrInfo::getRegSequenceLikeInputs(const llvm::MachineInstr &amp; MI, unsigned int DefIdx, int &amp; InputRegs) const'/>
<doc f='llvm/llvm/lib/Target/ARM/ARMBaseInstrInfo.h' l='45'>/// Build the equivalent inputs of a REG_SEQUENCE for the given \p MI
  /// and \p DefIdx.
  /// \p [out] InputRegs of the equivalent REG_SEQUENCE. Each element of
  /// the list is modeled as &lt;Reg:SubReg, SubIdx&gt;.
  /// E.g., REG_SEQUENCE %1:sub1, sub0, %2, sub1 would produce
  /// two elements:
  /// - %1:sub1, sub0
  /// - %2&lt;:0&gt;, sub1
  ///
  /// \returns true if it is possible to build such an input sequence
  /// with the pair \p MI, \p DefIdx. False otherwise.
  ///
  /// \pre MI.isRegSequenceLike().</doc>
