<def f='llvm/llvm/include/llvm/Support/TargetRegistry.h' l='595' type='void llvm::TargetRegistry::TargetRegistry()'/>
<doc f='llvm/llvm/include/llvm/Support/TargetRegistry.h' l='591'>// FIXME: Make this a namespace, probably just move all the Register*
  // functions into Target (currently they all just set members on the Target
  // anyway, and Target friends this class so those functions can...
  // function).</doc>
