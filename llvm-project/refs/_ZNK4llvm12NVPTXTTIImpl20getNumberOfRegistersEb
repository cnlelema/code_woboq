<def f='llvm/llvm/lib/Target/NVPTX/NVPTXTargetTransformInfo.h' l='69' type='unsigned int llvm::NVPTXTTIImpl::getNumberOfRegisters(bool Vector) const'/>
<doc f='llvm/llvm/lib/Target/NVPTX/NVPTXTargetTransformInfo.h' l='64'>// NVPTX has infinite registers of all kinds, but the actual machine doesn&apos;t.
  // We conservatively return 1 here which is just enough to enable the
  // vectorizers but disables heuristics based on the number of registers.
  // FIXME: Return a more reasonable number, while keeping an eye on
  // LoopVectorizer&apos;s unrolling heuristics.</doc>
