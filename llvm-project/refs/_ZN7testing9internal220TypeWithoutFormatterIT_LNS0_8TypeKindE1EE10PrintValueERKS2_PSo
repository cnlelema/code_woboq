<def f='llvm/llvm/utils/unittest/googletest/include/gtest/gtest-printers.h' l='173' ll='176' type='static void testing::internal2::TypeWithoutFormatter&lt;type-parameter-0-0, testing::internal2::TypeKind::kConvertibleToInteger&gt;::PrintValue(const T &amp; value, ::std::ostream * os)'/>
<doc f='llvm/llvm/utils/unittest/googletest/include/gtest/gtest-printers.h' l='166'>// Since T has no &lt;&lt; operator or PrintTo() but can be implicitly
  // converted to BiggestInt, we print it as a BiggestInt.
  //
  // Most likely T is an enum type (either named or unnamed), in which
  // case printing it as an integer is the desired behavior.  In case
  // T is not an enum, printing it as an integer is the best we can do
  // given that it has no user-defined printer.</doc>
