<dec f='llvm/lld/ELF/Driver.h' l='37' type='void lld::elf::LinkerDriver::compileBitcodeFiles()'/>
<def f='llvm/lld/ELF/Driver.cpp' l='1607' ll='1620' type='void lld::elf::LinkerDriver::compileBitcodeFiles()'/>
<doc f='llvm/lld/ELF/Driver.cpp' l='1600'>// This function is where all the optimizations of link-time
// optimization takes place. When LTO is in use, some input files are
// not in native object file format but in the LLVM bitcode format.
// This function compiles bitcode files into a few big native files
// using LLVM functions and replaces bitcode symbols with the results.
// Because all bitcode files that the program consists of are passed to
// the compiler at once, it can do a whole-program optimization.</doc>
