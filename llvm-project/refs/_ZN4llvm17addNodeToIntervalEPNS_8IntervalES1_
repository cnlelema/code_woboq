<def f='llvm/llvm/include/llvm/Analysis/IntervalIterator.h' l='82' ll='85' type='void llvm::addNodeToInterval(llvm::Interval * Int, llvm::Interval * I)'/>
<doc f='llvm/llvm/include/llvm/Analysis/IntervalIterator.h' l='76'>// addNodeToInterval - This method exists to assist the generic ProcessNode
// with the task of adding a node to the new interval, depending on the
// type of the source node.  In the case of a CFG source graph (BasicBlock
// case), the BasicBlock itself is added to the interval.  In the case of
// an IntervalPartition source graph (Interval case), all of the member
// BasicBlocks are added to the interval.</doc>
