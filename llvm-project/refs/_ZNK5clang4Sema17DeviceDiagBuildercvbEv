<def f='llvm/clang/include/clang/Sema/Sema.h' l='10449' type='bool clang::Sema::DeviceDiagBuilder::operator bool() const'/>
<doc f='llvm/clang/include/clang/Sema/Sema.h' l='10439'>/// Convertible to bool: True if we immediately emitted an error, false if
    /// we didn&apos;t emit an error or we created a deferred error.
    ///
    /// Example usage:
    ///
    ///   if (DeviceDiagBuilder(...) &lt;&lt; foo &lt;&lt; bar)
    ///     return ExprError();
    ///
    /// But see CUDADiagIfDeviceCode() and CUDADiagIfHostCode() -- you probably
    /// want to use these instead of creating a DeviceDiagBuilder yourself.</doc>
