<dec f='llvm/llvm/include/llvm/Transforms/IPO/Attributor.h' l='532' type='llvm::ChangeStatus llvm::AbstractAttribute::updateImpl(llvm::Attributor &amp; A)'/>
<doc f='llvm/llvm/include/llvm/Transforms/IPO/Attributor.h' l='525'>/// The actual update/transfer function which has to be implemented by the
  /// derived classes.
  ///
  /// If it is called, the environment has changed and we have to determine if
  /// the current information is still valid or adjust it otherwise.
  ///
  /// \Return CHANGED if the internal state changed, otherwise UNCHANGED.</doc>
<use f='llvm/llvm/lib/Transforms/IPO/Attributor.cpp' l='154' u='c' c='_ZN4llvm17AbstractAttribute6updateERNS_10AttributorE'/>
