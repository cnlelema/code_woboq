<def f='llvm/llvm/include/llvm/Support/GenericDomTreeConstruction.h' l='1579' ll='1595' type='static bool llvm::DomTreeBuilder::SemiNCAInfo::IsSameAsFreshTree(const DomTreeT &amp; DT)'/>
<doc f='llvm/llvm/include/llvm/Support/GenericDomTreeConstruction.h' l='1572'>// Check if the given tree is the same as a freshly computed one for the same
  // Parent.
  // Running time: O(N^2), but faster in practise (same as tree construction).
  //
  // Note that this does not check if that the tree construction algorithm is
  // correct and should be only used for fast (but possibly unsound)
  // verification.</doc>
